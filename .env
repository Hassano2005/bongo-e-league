# 1. Remove .env from git (security!)
git rm --cached .env
echo ".env" >> .gitignore

# 2. Stage and commit fixes
git add .gitignore server.js
git commit -m "fix: correct db require path, secure .env"

# 3. Push to trigger Render deploy
git push origin main
